<!--
详细度要求（写本文件时遵守）

本文件是该 feature 的"全局 source of truth"，被所有模块 md 引用。**任何被
多个模块共用的概念**（共享 schema / 共享 API 契约 / 共享术语 / 跨模块约定）
都要在本文件里写完整，不要让模块 md 各自重复定义、各自解释。

- §3.1 共享数据结构：完整字段表（名 / 类型 / 必填 / 解释）
- §3.2 共享 API 契约：URL + 全部字段，调用方/实现方双向引用
- §3.3 共享术语：每个术语一句精确定义，禁止"约定俗成"
- §5.3 派发前必须强调：项目特定红线全列（不要省略）

不限字数。模块 md 写得详细 = 本文件先把全局都写清楚。对应 SKILL.md Hard rule #10。
-->

# <FEATURE_NAME> · 00 · 总体规划

> 版本：v<N>
> 创建于：<YYYY-MM-DD>
> 权威性：本文档是该 feature 的**唯一权威源**，所有模块 md 必须遵循；如有冲突以本文档为准
> 输出位置：`<output_dir>/`（由 Phase 0 用户决定，常见 `.todolist/<feature>-v<N>/` 或 `docs/plans/<feature>-v<N>/`）

---

## 0. 背景

<2-3 段，说明：>
- 为什么做这个需求（业务驱动 / 技术驱动 / 合规驱动）
- 现状缺口（已有什么，还缺什么）
- 预期产出（功能边界，里程碑）
- 关键非功能需求（性能 / 安全 / 可观测）

> 如果项目根没有 `AGENTS.md` / `CLAUDE.md` / `.cursorrules` 等项目级配置文件，
> 在本节末尾追加一段：
> ```
> ⚠️ 本项目未发现项目级约定文档（AGENTS.md / CLAUDE.md / .cursorrules）。
> ultra-plan 用通用模板生成。建议执行前补一份项目级约定，否则各 subagent 实现易飘。
> ```

---

## 1. 模块清单

| # | 模块 | 简述 | 耦合度 | 波次 | 文档 |
|---|---|---|---|---|---|
| 01 | <模块名> | <一行描述> | 高/中/低 | W1 | [01_<模块>.md](./01_<模块>.md) |
| 02 | ... | ... | ... | ... | ... |
| ... | | | | | |

---

## 2. 模块依赖图

```
W1 (基础)        W2 (依赖 W1)       W3 (集成)
  │                  │                  │
  ├─ 01 模块A ──────┬─ 03 模块C ──────┬─ 06 集成测试
  ├─ 02 模块B ──────┘                  │
  │                  ┌─ 04 模块D ──────┤
  │                  │                  │
  │                  └─ 05 模块E ──────┘
```

**波次说明**：
- W1：彼此独立，可并行
- W2：依赖 W1 完成
- W3：集成 / 端到端测试 / 联调

---

## 3. 跨模块契约

### 3.1 共享数据结构

<列出多模块共用的 schema / Pydantic model / TypeScript interface / GraphQL type 等>

### 3.2 共享 API 契约

<列出被多模块调用的 endpoint，避免每个模块 md 重复定义>

| Method | URL | 调用方模块 | 实现方模块 |
|---|---|---|---|
| <METHOD> | `<endpoint>` | <模块编号> | <模块编号> |

### 3.3 共享术语

<术语统一定义，避免各模块 md 自创不同名称>

| 术语 | 定义 |
|---|---|
| <术语> | <定义> |

---

## 4. 技术栈与全局约定

> 从项目级配置（AGENTS.md / CLAUDE.md / package.json / pyproject.toml 等）抽取。无配置时填"项目未明确约定"。

### 4.1 后端

- 语言 / 框架：<>
- 数据库 / ORM：<>
- 缓存 / 队列：<>

### 4.2 前端

- 框架：<>
- 状态管理：<>
- 样式方案：<>

### 4.3 全局约定

- API 风格：<RESTful / RPC-style / GraphQL / gRPC / etc>
- 响应格式：<信封约定>
- 错误码规范：<>
- 日志 / 追踪：<>

---

## 5. 派发指令（Subagent 执行手册）

> 本节供执行阶段（Phase 5）的主 agent 派发 subagent 时使用。**新开 Codex 窗口的开发者**：让 Codex 先读完本文档全部，再按本节派发。

### 5.1 派发顺序

按 §2 波次图，**波内并行 / 波间串行**。每完成一波，主 agent 自检后再启动下一波。

### 5.2 单个模块的派发模板

```
角色：<后端 / 前端 / 全栈 / 测试工程师>
任务：<MD序号> <模块名>

必读清单（必须复述）：
  主权威：
    <绝对路径>/<output_dir>/<MD序号>_<模块>.md
    <绝对路径>/<output_dir>/00_总体规划.md
  上下文：
    <项目 AGENTS.md / CLAUDE.md 路径>（如存在，AGENTS.md 优先）
    <相关 skill 路径，如有项目特定 skill>
    <相关 memory 路径，如有>
  参考：
    <现有类似实现的代码路径>

首条回复必须 ≤ 150 字复述：
  1. 主权威 md 的绝对路径
  2. 要建/改的端点（完整 URL）
  3. 要动的表 / schema（名称 + 关键变更）
  4. 要导出的前端符号（如适用）
  5. 依赖的其他模块符号

派发前必须强调（项目特定红线）：
  - <从 §5.3 复制>
```

### 5.3 派发前必须强调（项目特定红线）

> 由 Phase 0.1 项目探测时从项目级配置抽取后填入。
> 抽取标准见 ultra-plan skill 的 [项目适配指南](~/.codex/skills/ultra-plan/references/项目适配指南.md) §2 八类目。

**通用条目**（任何项目都贴）：
- 严格遵循项目级 AGENTS.md / CLAUDE.md / .cursorrules 全部约定
- 不修改项目级配置文件本身
- 改 endpoint 后跑项目要求的文档 / OpenAPI 生成命令（如有）
- 测试看开发者决定（与项目测试约定一致）

**项目特定条目**（按 8 类目抽取）：
- API 风格：<>
- 响应格式：<>
- env 管理：<>
- 目录约定：<>
- 测试要求：<>
- 数据库 / 存储约定：<>
- 资源落盘：<>
- 安全 / 合规：<>

> 项目未明确的类目留空或标"无明确约定"，**不要编造**或从其他项目搬来。

---

## 6. 验收红线（全局）

> 测试覆盖率、E2E 工具、CI 命令等从项目配置抽取。无配置时给出推荐默认。

### 6.1 后端

- [ ] 单元测试 / 集成测试通过
- [ ] 覆盖率达标（项目阈值，无则默认 ≥ 70%）
- [ ] API 文档已更新（如项目用 OpenAPI / Swagger / GraphQL schema）
- [ ] 关键模块覆盖 happy path + 鉴权失败 + 参数错误三条

### 6.2 前端

- [ ] E2E happy path 跑通（截图存项目约定位置）
- [ ] 主流程边界 case 测试通过
- [ ] type-check / lint 全绿

### 6.3 文档与可观测

- [ ] 关键决策有 decision log（如项目有此约定）
- [ ] 新增 endpoint 有 docstring / OpenAPI 描述
- [ ] 新增的 env key 已写进项目约定的 env 模板

---

## 7. 非目标（明确不做）

- <不做项 1：理由>
- <不做项 2：理由>
- ...

---

## 8. 风险与对策

| 风险 | 概率 | 影响 | 对策 |
|---|---|---|---|
| <风险描述> | 高/中/低 | 高/中/低 | <对策> |

---

## 9. 进度追踪

> 主 agent 在 Phase 5 执行过程中更新本节

| 模块 | 状态 | 完成日期 | 备注 |
|---|---|---|---|
| 01 | ⬜ 未开始 / 🟡 进行中 / ✅ 完成 / ⛔ 阻塞 | - | - |
| ... | | | |
