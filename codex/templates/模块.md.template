<!--
详细度要求（写本文件时遵守，写完后保留此注释作为 source-of-truth 提醒）

本 md 是另起 AI session 的执行者的**唯一权威源**。每一节都要详细到执行者
"零参考即可实现"：

- §2 现状定位：列全相关 file:line，不只是几个示例
- §3 实现指引：完整目录结构 + 函数签名（含返回类型 + 一句 docstring）+ 关键算法伪代码
- §4 API 契约：URL / 请求字段（类型 / 必填 / 解释）/ 响应字段（同）/ 错误码全列
- §6 验收红线：测试场景具体到"用户输入 X → 期望响应 Y"
- §7 边界 & TBD：所有产品决策项列清，附默认推荐答案

不限字数。模块 md 越详细，执行者越不需要回头问。对应 SKILL.md Hard rule #10。
-->

# <MD序号> · <模块名>

> 负责人：Subagent <代号>
> 覆盖需求：[00_总体规划.md](./00_总体规划.md) **§1 模块清单 #<MD序号>**
> 前置：<依赖模块编号 + 简明理由>
> 耦合度：高 / 中 / 低

---

## 1. 范围与目标

**要做**：
- <子目标 1，具体可验证>
- <子目标 2>
- <子目标 3>

**不做**（明确边界，避免范围蔓延）：
- <延后项 1：理由>
- <延后项 2>

---

## 2. 现状定位

| 项 | 状态 | 位置 |
|----|------|------|
| <核心表 / service / 组件> | ✅ / ⚠️ / ❌ | <绝对路径或 file:line> |
| <相关 endpoint> | ✅ / ⚠️ / ❌ | <文件:行号> |
| <现有可复用代码> | ✅ | <文件:行号> |

**状态符号**：
- ✅ 已存在可直接用
- ⚠️ 已存在但需修改
- ❌ 不存在需新建

---

## 3. 实现指引

### 3.1 后端（如适用）

> 目录结构按项目约定填写，从项目根的 AGENTS.md / CLAUDE.md / 现有目录布局抽取。

**目录结构**：
```
<按项目约定的目录布局填>
├── <文件 1>
├── <文件 2>
└── ...
```

**数据 schema 变更**：
- 新表 / 新 collection：<名称>，关键字段：<>
- 修改表 / collection：<名称>，新增字段：<>
- 索引：<>

**关键函数 / 类签名**：
```<语言>
async function <name>(...): <return>
    """<一句话说明>"""
    ...
```

### 3.2 前端（如适用）

**路由 / 页面**：
- 新增：`<route>` → `<file path>`

**复用组件清单**：
- `<component_name>` from `<path>` —— 用途

**新增组件**：
- `<component_name>` —— 职责

**状态管理**：
- store / hook 名称：<>
- 关键 action：<>

**API 调用层**：
- queries / fetcher 文件新增 endpoint：<>

---

## 4. API 契约

| Method | URL | 请求 | 响应 | 用途 |
|--------|-----|------|------|------|
| <METHOD> | `<endpoint>` | `{...}` | `<项目响应信封格式>` | <用途> |

**错误码**（如有特定）：
- `<code>`: <语义>

---

## 5. 前置依赖

- **<MD序号> <模块名>** —— 因为本模块 <调用 / 共享 schema / 复用组件>
- ...

---

## 6. 验收红线

### 6.1 后端
- [ ] 单元 / 集成测试覆盖：<具体路径> 行覆盖率 ≥ <项目阈值>
- [ ] integration test：happy path + 鉴权失败 + 参数错误 三条
- [ ] API 文档（OpenAPI / Swagger / GraphQL schema）已更新
- [ ] <项目约定的其他后端验收>

### 6.2 前端
- [ ] E2E happy path：<具体路径，例：登录 → 跳到 X 页面 → 完成 Y 操作 → 看到 Z 结果>
- [ ] 边界 case：<列 2-3 个>
- [ ] type-check / lint 全绿

### 6.3 集成
- [ ] 端到端：<具体场景>
- [ ] 与 <其他模块> 联调：<>

---

## 7. 边界 & TBD

> 需产品决策的留白、可选项、未来扩展点

- **TBD**：<问题描述> —— 待 <谁> 决策
- **可选**：<功能> —— 当前不做，未来可加
- **风险**：<> —— 对策：<>

---

## 8. 派发前必须强调（执行 subagent 必读）

> 主 agent 派发执行 subagent 时把以下要点贴进 prompt。
> 内容 = `00_总体规划.md §5.3` 全文复制 + 本模块特有红线（如有）。

**通用条目**：
- 严格遵循项目级 AGENTS.md / CLAUDE.md / .cursorrules 全部约定
- 不修改项目级配置文件本身
- 改 endpoint 后跑项目要求的文档 / OpenAPI 生成命令（如有）

**项目特定条目**：
<从 00_总体规划.md §5.3 复制>

**本模块特有红线**（如有）：
- <例：本模块涉及隐私字段，必须按项目加密约定加密>
- <例：本模块是管理后台，权限模型与用户端不同>
- <例：本模块涉及大表分页，禁用 OFFSET 改用 cursor>
